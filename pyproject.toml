[project]
name = "weightloss-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=14.2.0",
    "typer>=0.21.0",
]
[project.scripts]
weight = "weightloss_cli.main:app"